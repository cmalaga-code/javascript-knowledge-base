// returns an array as a string
// does not change the org array inplace
// any seperator can be specified
// array.join(separator)
// https://www.w3schools.com/jsref/jsref_join.asp

const groceryList = ["apple", "Orange", "Banana"];

// by default it is a , as an argument
let text = groceryList.join(" and ");
