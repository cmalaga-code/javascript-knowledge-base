// similar to pop but the reverse
// removes the first element of an array
// does this inplace
// returns the shifted array
// array.shift()
// https://www.w3schools.com/jsref/jsref_shift.asp

const age = [10, 11, 12];

age.shift();

console.log(age);