// returns a string
// the array is converted to a string
// this is not done inplace & returns a copy of the array as a string
// array.toString()
// https://www.w3schools.com/jsref/jsref_tostring_array.asp

const exampleArray = [1, 3, 5, 6];

let text = exampleArray.toString();

console.log(text);